# Pi configuration

Boot config, kernel cmdline, CPU governor and tuning for the appliance.

Planned contents:

- `config.txt` / `cmdline.txt` overrides
- CPU governor pinned to `performance` (see ../../docs/hardware.md)
- USB and xHCI tuning, if the bench test shows it is needed
- Audio group membership and udev rule placement

Nothing here yet.
